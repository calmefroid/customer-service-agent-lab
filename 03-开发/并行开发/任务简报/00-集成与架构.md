# 00 对话任务简报：集成与架构

## 可直接发给新对话的首条消息

> 你负责“客服 Agent 实验室”的集成与架构，不承包单个大功能。请先阅读 `/Users/lengwen/Workspace/ai-project/projects/客服Agent实验室/04-开发/并行开发/README.md`、PRD 和当前代码。第一步建立 Git 基线、公共契约和模块所有权；之后负责审查、合并其他功能分支并持续跑全量回归。不要替功能对话越界开发，也不要在契约未冻结前启动并行合并。

## 工作范围

- 初始化仓库、基线提交、模块 branch / worktree。
- 冻结公共事件、工具、确认、RAG 和 Trace 契约。
- 设计注册式编排接口，逐步拆解巨型 `mock-orchestrator.ts`。
- 审批 `变更申请`，统一修改共享热点文件。
- 合并各模块、解决冲突、维护导航和文档。
- 每次合并执行全量测试、构建和关键 E2E。

## 独占文件

- `src/lib/contracts.ts`
- `src/lib/mock-orchestrator.ts`
- `src/app/api/chat/route.ts` 的集成部分
- `package.json`、锁文件、`.env.example`
- 根 README、PRD 状态和本并行计划

## 第一阶段交付

1. 当前代码基线提交。
2. 六个 worktree 和分支。
3. `contracts-v1`：AgentEvent、ToolResult、ConfirmationRequest、KnowledgeRetrievalResult、TraceEvent。
4. 模块注册接口和兼容当前 Mock 的适配层。
5. CI 等价本地命令：测试 + 构建。

## 合并顺序

`01 Runtime` → `02 Business` → `03 Knowledge` → `04 Consumer` → `05 Operations` → `06 Evals`。

每次只合并一个分支并立即回归；不要把多个大型分支一次性合并后再排错。

## 验收

- 当前功能不回退。
- 公共契约只有一个权威定义。
- 功能模块不再直接互相 import 页面或具体 Mock 数组。
- 消费者端不返回 TraceDebugContext。
- 全量测试与构建通过。

## 状态

```text
状态：第一阶段完成，等待功能分支交付
基线提交：73ab908
交付提交：contracts-v1（以 Git 标签为准）
阻塞或契约申请：无
```
